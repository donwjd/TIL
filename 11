def solution(n):
    return sum(map(int, list(str(n))))

n을 문자열로 바꿔서 list() 함수에 담으면 각 자리수를 분리해서 리스트로 담을 수 있다.
그리고 map() 함수를 사용하면 입력되는 리스트들에 모두 일정한 함수를 적용시킬 수 있는데, 
무슨 말이냐면 위 코드에서 map(int, list)를 하면 list에 있는 모든 요소를 int() 함수에 넣은 결과값과 같다는 것이다.
